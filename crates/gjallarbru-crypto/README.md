# gjallarbru-crypto

Future `no_std` provider boundaries for integrity, credential derivation,
constant-time verification, authenticated nonces, and server tokens. This
crate is licensed under `MIT OR Apache-2.0` for eventual crates.io publication.

It does not implement TLS or DTLS and currently exposes no API.

