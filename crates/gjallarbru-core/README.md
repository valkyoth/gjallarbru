# gjallarbru-core

Future deterministic, bounded, Sans-I/O STUN/TURN server state for Gjallarbru.
This crate is `no_std`, forbids unsafe code, and is licensed under
`MIT OR Apache-2.0` for eventual crates.io publication.

The crate is currently a repository-foundation scaffold and exposes no state
engine API.

